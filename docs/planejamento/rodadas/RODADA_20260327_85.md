# RODADA 20260327 85

## Objetivo

Registrar o fechamento do subciclo final de navegacao/site, atualizar o
`changelog` e consolidar o encerramento da frente de saneamento documental
legado.

## Contexto

Depois da reorganizacao semantica da arvore `docs/` e da validacao estrita do
MkDocs, faltava apenas registrar formalmente:

- o polimento final de navegacao do site documental
- a publicacao manual da documentacao saneada na branch `gh-pages`
- o encerramento desta frente como esforco amplo de saneamento

## Intervencoes executadas

- atualizacao de `docs/changelog.md` para absorver o subciclo final de polimento
  do site e a publicacao em `gh-pages`
- ajuste da secao `Em andamento` para refletir apenas curadoria residual, sem
  nova frente ampla aberta
- consolidacao da rastreabilidade da frente ate as rodadas `84` e do site
  publicado em `https://rib-thiago.github.io/showtrials-tcc/`

## Validacao

Permanecem como base de validacao desta etapa:

```bash
poetry run mkdocs build --strict
poetry run mkdocs gh-deploy
```

Ambos os comandos ja haviam concluido com sucesso ao final do subciclo.

## Resultado

O fechamento da frente fica documentalmente consolidado:

- changelog alinhado ao estado real do projeto e do site
- publicacao do GitHub Pages explicitamente registrada
- transicao de uma frente ampla de saneamento para um regime residual de
  curadoria fina
